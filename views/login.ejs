<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center px-4"
    >
      <form
        action="/login"
        method="post"
        class="bg-zinc-800 w-full max-w-md p-8 rounded-xl shadow-lg space-y-5"
      >
        <h2 class="text-2xl font-bold mb-4 text-center">Login your Account</h2>
        <input
          type="email"
          name="email"
          placeholder="Email"
          class="w-full px-4 py-3 bg-zinc-700 rounded-md outline-none focus:ring-2 focus:ring-blue-500 placeholder-gray-400"
        />

        <input
          type="password"
          name="password"
          placeholder="Password"
          class="w-full px-4 py-3 bg-zinc-700 rounded-md outline-none focus:ring-2 focus:ring-blue-500 placeholder-gray-400"
        />

        <input
          type="submit"
          value="Login Account"
          class="w-full py-3 bg-blue-600 hover:bg-blue-700 rounded-md font-semibold text-white transition-all duration-300 cursor-pointer"
        />
      </form>
    </div>
  </body>
</html>
